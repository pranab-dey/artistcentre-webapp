import React from 'react';

function Divider() {
  return (
    <hr
      style={{
        marginTop: '1rem',
        marginBottom: '1rem',
        border: 0,
        borderTop: '1px solid rgba(0, 0, 0, 0.5)',
      }}
    />
  );
}

export default Divider;
