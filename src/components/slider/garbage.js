{
	/* {data?.map((i) => ( */
}
{
	/* <SwiperSlide
					// key={i.id}
					style={{
						cursor: 'pointer',
						border: '10px solid green',
						height: 'auto',
						// width: '100px',
					}}
					onClick={(e) => handleClick(e, i.id)}
					className='swiper-slide'>
					<div
						style={{
							borderRadius: '15px',
							boxShadow: '0px 5px 10px rgba(0, 0, 0, 0.1)',
							maxWidth: '300px',
							height: '200px',
						}}
						// className='d-flex justify-content-space-around align-items-center'
					>
						<Image
							src={
								// i.event_image_url ||
								// i.group_image_url ||
								// i.artist_image ||
								// i.profile_picture ||
								data.venue_image || '/assets/no-image.jpeg'
							}
							width={'300px'}
							height={'200px'}
							// alt={`image-slide-for-${i.id}`}
							style={{
								borderRadius: '15px',
							}}
						/>
					</div>

					{conditionalImageFooter(type, {
						title:
							// i.event_name ||
							// i.group_name ||
							// i.artist_name ||
							data.venue_name || 'Unknown',
						// artistName: i.artist?.[0].artist_name ?? '',
					})}
				</SwiperSlide> */
}
{
	/* ))} */
}
